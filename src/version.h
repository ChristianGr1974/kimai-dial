#pragma once

// Single source of truth for the firmware version, shown on the boot screen
// and the web setup page footer. Bump manually when cutting a release/tag.
constexpr const char *FIRMWARE_VERSION = "1.0.0";
